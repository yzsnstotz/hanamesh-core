# AC-24 · plugin-identity v0.1.0-rc.1

**总体状态：PARTIAL。真实宿主／服务／视觉等必需门仍为 BLOCKED，不记 DELIVERED，更不记 ACCEPTED。**

## 1. 派发、来源与实际环境

本轮执行附件 `00_DISPATCH_PROMPT.md`，目标仓名／目录名／bundle 名均为 `hanamesh-plugin-identity`，分支 `main`。输入 ZIP SHA-256：`85114384f63e86d1a37e17dfa3bc0d0e3c02a34aa049e0527c5abcd0ac3c7a92`。输入 STATUS §2A 的 plugin-identity 为待开工，srv-identity 已 ACCEPTED 且明确解锁该模块；ui-kit 已验收版本为 rc.3，但其产物不在本 ZIP 内。没有从历史记忆补齐缺失的接口或包。

已读附件的 ENVIRONMENT、DELIVERY_RULES、STATUS §2A、MODEL_POLICY §1/3、PROCEDURE、模块 brief、RUNTIME_PROFILE、CONSISTENCY、客户端总则、srv-identity / ui-kit 指定条目、指定 PRD 条款与适用 learnings。没有复制一套 DSH 安装／身份／会话权威。所有修改只在本仓及本轮临时测试目录内。

实际 Linux x64；Node 22.16.0、npm 10.9.2、TypeScript 5.8.3；pnpm / dsh / psql 不存在。目标 Node 24.13.1、pnpm 10.33.0、TypeScript 5.9.3、macOS arm64 **不是实测版本**。目标 DSH：0.1.5-alpha.1 @ 5dda764ed3aa172535a7967b06ff95d9cbfe536a。

`environment.log` 有命令输出与输入文档指定的 runtime、ui-kit、upstream、research rt 目录存在性检查；均不存在。`dsh-install-attempt.log` 对候选 tarball 的实际 `dsh plugin add` 尝试返回 ENOENT，使用独立随机 DSH_HOME，未启动宿主或触碰个人 profile。`host-types-blocked.log` 记录完整宿主类型检查前置缺少 node 类型；未通过。早期网络 EAI_AGAIN 仅保留工具摘要于 `network-note.md`，不伪装为尚存的 npm 原始日志。

## 2. 逐项矩阵（不以替身关闭真实门）

| ID | 状态 | 证据类型与原始输出 | 本轮结论／未关闭条件 |
|---|---|---|---|
| D01 | BLOCKED | SOURCE、OFFLINE_CORE_BUILD、PACKAGE_SMOKE；`offline-build.log`、`npm-pack.json`、`standalone-package.log`、`adapter-fixture.tap`；宿主尝试 `dsh-install-attempt.log` | 实际编译、npm pack、独立 npm 安装入口冒烟成功；没有真实 DSH profile 安装与启动，目标工具链构建亦未通过。 |
| D02 | BLOCKED | CONTRACT_FIXTURE：`tests.tap`、`test/controller.test.mjs`；环境 `environment.log` | 合成登录→主体→带会话请求通过；未对真实 SRV-01 登录，未建账户／执行 PostgreSQL。 |
| D03 | BLOCKED | CONTRACT_FIXTURE：`tests.tap` 中 logout / expired / server-revoked / resourceUnauthorized；`mutations/` | 客户端状态和合成资源 401 已覆盖；没有真实服务端的撤销／过期拒绝证据。 |
| D04 | BLOCKED | CONTRACT_FIXTURE：A→B 合成 403，`tests.tap`；`mutations/accept-resource-403.tap` | 资源 403 被正确传播；没有真实 A/B 账户及 API 负测，不能宣称资源端隔离已验证。 |
| D05 | BLOCKED | CONTRACT_FIXTURE：forged sidecar / header / principal cases，`tests.tap` | 篡改 sidecar 不能恢复身份，SDK 不收任意 header，不发自报主体；真实资源端面对伪造请求仍须补测。 |
| D06 | PASS | SOURCE：`source-and-log-audit.log`、`public-types.log`；README / SECURITY 边界 | 没有 JWT／签名／grant／entitlement 发行接口或实现；公开 SDK 无凭据或权限发行方法。grep 为候选源代码扫描，另辅以人工契约审阅。 |
| D07 | BLOCKED | CONTRACT_FIXTURE：直接调用 request、同源 API 与无宿主认证页面；`tests.tap`、`adapter-fixture.tap` | 测试不依赖 UI 隐藏且直接调用会拒绝；还未在真实 DSH 与真实资源 API 上去掉隐藏后复验。 |
| D08 | PARTIAL | CONTRACT_FIXTURE_MUTATION：`mutations.log`、`mutations/*.tap` | 2 个 guard／拒绝映射变异 exit 1、not ok、ERR_ASSERTION；基线通过、无导入错误。只证明本仓替身测试，真实 D03/D04 链路仍 BLOCKED。 |
| D09 | BLOCKED | SOURCE / CONTRACT_FIXTURE：五态标签、语义 HTML、无不安全 DOM 存储；`tests.tap` | ui-kit rc.3 tarball／digest 缺失，未消费其样式、无真实截图。页面是功能界面，不是设计 token 替代品。 |
| D10 | PARTIAL | CONTRACT_FIXTURE：outage / privateWork preserved / no extra protected requests；`tests.tap` | 本仓新操作 fail-closed、状态通知、不删除私有工作已测；真实业务 owner 收敛运行任务及数据保留未联验。 |
| D11 | BLOCKED | SOURCE / ADAPTER_FIXTURE：`adapter-fixture.tap`，single domain 一次 global.set | 没有写入 DSH session 事件；真实 sidecar 与 DSH 自身读取 session 的反向验收未运行。 |
| D12 | PARTIAL | SOURCE / CONTRACT_FIXTURE：`source-and-log-audit.log`、`tests.tap`、`public-types.log` | 当前原始测试日志和 diagnostics 无合成密码／cookie 值；没有公开 raw auth。真实浏览器、宿主日志、诊断包仍需检查。源码测试 fixture 中的合成常量不冒充真实秘密。 |
| D13 | BLOCKED | INPUT_HASH：`dependency-hashes.log` | 输入 SRV tarball／契约和 94 个宿主公开声明保持原哈希；真实 pinned DSH checkout 不存在，无法提交所要求的 live diff/hash。不能拿输入类型哈希替代上游目录验收。 |
| D14 | PARTIAL | STANDALONE_SOURCE_BUILD / PACKAGE_SMOKE：`standalone-package.log` | 在随机临时目录复制本仓允许文件，未含兄弟源码，strict 核心编译、公开类型与 tarball 入口通过；ui-kit 消费及完整目标构建未完成。 |
| X01 | PASS | LOCAL_CHECKER：`tests.tap` 的 consistency declaration case，`consistency.json` | 声明合法：1 个完整镜像 group、2 个跨介质 boundary，名称／事实／介质／安全侧均校验；无公共 checker 时使用本仓检查。 |
| X02 | BLOCKED | REAL_PROCESS_FIXTURE_MEDIUM：`crash/group-before-rename.json`、`crash/group-after-rename.json`、`tests.tap` | 真 SIGKILL 在测试原子文件 fsync 前后检查点执行，完整旧／新镜像通过；不是 DSH storage-domain 实际 backend，必需真实介质门未关闭。 |
| X03 | BLOCKED | REAL_PROCESS_FIXTURE_MEDIA + MUTATION：`crash/login-*`、`crash/logout-*`、`tests.tap` | 生产 controller 子进程真 SIGKILL，替身远端已提交事实；两条安全侧基线通过，反序各触发 ERR_ASSERTION。真实 DSH＋SRV-01／DB 路径未跑。 |

## 3. 实际输出与可复跑命令

以下均为本轮真实执行；脚本只记录命令、退出码、版本、digest、进程信号与断言，不硬编码验收状态。

| 命令 | 实测结果 | 原始文件 |
|---|---|---|
| `node scripts/preflight.mjs` | 当前工具和指定目录可用性；dsh/psql/pnpm 缺失 | `environment.log` |
| `npm run build` | exit 2，TARGET_TOOLCHAIN_MISMATCH | `target-build.log` |
| `tsc -p tsconfig.host.json` | exit 2，缺少 node 类型入口；宿主类型门未执行完毕 | `host-types-blocked.log` |
| `npm run build:offline` | exit 0，strict 便携 TS 编译，adapter / UI JS syntax check | `offline-build.log` |
| `npm test` | exit 0，**43/43 顶层测试通过** | `tests.tap` |
| `npm run test:mutation` | exit 0；38 项基线测试通过，两个变异子进程各 exit 1 + ERR_ASSERTION | `mutations.log`、`mutations/baseline.tap`、两份 mutant TAP |
| `npm run check:contracts` | exit 0；只针对框架无关公开 SDK，包含 @ts-expect-error 负测 | `public-types.log` |
| `node scripts/verify-inputs.mjs` | exit 0；SRV hash、contracts hash、94 声明 hash 一致 | `dependency-hashes.log` |
| `npm pack --pack-destination artifacts --json` | exit 0；npm 默认候选文件名 | `npm-pack.json`、`npm-pack.stderr.log` |
| `node scripts/check-package.mjs` | exit 0；独立源码目录编译；临时 npm 安装 1 包及出口冒烟。**使用 legacy-peer-deps 故意不安装宿主，不执行 apply** | `standalone-package.log` |
| 实际 `dsh plugin add <tarball>` | ENOENT，进程未启动 | `dsh-install-attempt.log` |
| token/权限发行候选扫描与合成凭据日志扫描 | grep exit 1，无匹配；不是“全面无漏洞”的证明 | `source-and-log-audit.log` |

宿主 fixture 子套件 `adapter-fixture.tap` 有 4 个装配／关闭测试，是 43 个顶层测试中一个 wrapper 的内部测试，不与总数相加冒充更多独立门。所有 HTTP Response / Headers 用的是实际 Node Fetch 类型，但远端响应是合成的，不是抓取真实服务器。

强杀一共覆盖 group 的提交前／后 2 个进程，以及 login/logout 基线与反序 4 个进程。`crash/*.json` 保留实际 pid、SIGKILL、退出信号、本地镜像和远端事实；`crash/*-reverse.tap` 的红色是**预期被杀变异**，不是当前产品基线失败。未运行任何付款、真实私钥、生产请求或外部模型调用。

### 中途失败与修复记录

早期核心 TS 出现路由字面量 includes 参数类型错误，改为只读 string 数组后 strict 编译通过；未开启 skipLibCheck 或 noCheck。`initial-tests.tap` 是当时 22 项核心基线。

`history/edges-first.tap` 保留静态扫描把注释当实际 localStorage 使用的一次误报，已修正注释。`history/crash-harness-first.tap` 与 `history/nested-checker-first.log` 保留子 node:test 继承环境导致递归跳过的失败；移除该环境变量，并要求 checker 确实运行一个测试后修复。每个问题均依据输出定点修复，没有盲目重复。后续回归同时加入超时迟到 cookie、账户切换、旧会话撤销未知不被新登出覆盖等保护。

## 4. 锁定产物与兼容记录

- 本模块候选：`artifacts/hanamesh-plugin-identity-0.1.0-rc.1.tgz`。
- 本模块 tarball SHA-256：`17e69910cf2a6fd74cd559975da1ccc333255bb22c806a62f7c53e26d3d3e957`。`artifacts/SHA256SUMS` 可从仓根验证。
- SRV-01：0.1.0-rc.1，SHA-256 `19b54a32841647a99ed04883833461c0702f2b073567204d718c2e042cdce557`；原 artifact 和公开 contracts 只读消费。
- DSH：0.1.5-alpha.1 @ `5dda764ed3aa172535a7967b06ff95d9cbfe536a`；94 个附件声明哈希在 `deps/HOST_API.sha256.json`。完整宿主运行依赖未解决。
- ui-kit：期望 0.1.0-rc.3，输入记录源码 `d1e1a50`，tarball 与 SHA 未提供。不能登记一个不存在的组合 digest。

仓内 package-lock 是真实离线生成的本插件／peer 声明 lock，不是完整宿主解析 lock；这项限制不隐藏。构建的 TS 实际为 5.8.3；后续目标环境重新构建若内容改变，必须升级候选版本。候选标签仅 `v0.1.0-rc.1`，没有正式 v0.1.0。源码 commit 由本仓 `git rev-parse HEAD` 和 bundle 内 main/tag 确认，避免在提交内容里自引用其自身 hash。

远程不修改共享 STATUS，不生成 STATUS.patch／DELIVERY.json／OUTCOME.json／START_HERE；回收者再把本矩阵、下方 checkpoint 和实际 commit/digest 合入唯一状态台账。

## 5. 缺口、owner 与恢复范围

本机回收者负责拿到固定 Node/pnpm/TS、真实 DSH runtime 和干净 upstream；使用独立 DSH_HOME 与 web profile，补授权测试账户／真实 SRV-01 与必要 DB 环境，再对矩阵逐项取证。不得动个人 3080、个人 ~/.dsh 或自动创建生产资源。

ui-kit 产物补齐后，还需本模块的样式消费改动并跑真实五态截图；**不只是安装缺少的包就可把 D09 改 PASS**。宿主类型门需要用实际依赖完成；任何公开声明／真实行为冲突只修改本插件，不 fork 上游或发明替代服务。若条件齐全但实际验收暴露功能缺口，按 PROCEDURE 做范围缺口回收，不重写整个模块。

## 6. 本轮模型与人工介入

会话身份：GPT-6 Astra Pro；没有可用的模型切换动作，未声称已切到默认 Sonnet 5 High。工具未提供 backend selector / effort / token 用量 / 费用，全部记 unknown。未额外调用任何模型、无付费外部服务。关键编译与测试 harness 问题各一次有依据修复；没有请求用户逐个调度内部任务，没有 push、建远端或发布。

## 7. Checkpoint（由本机回收时登记）

**做了什么：** 完成登录功能页面、版本化 SDK、宿主公开 API 适配、单镜像 sidecar、凭据内存隔离、失效／撤销／并发保护、离线 strict 编译、43 项测试、guard 与反序变异、真实测试子进程强杀、独立源码与 tarball 冒烟；保留真实日志、learning、候选 npm 产物与本地 git 提交。

**下一步是什么：** 在本机固定环境做一次回收判定；先补 ui-kit rc.3 锁定产物与真实测试依赖，再补样式接入并重跑 AC-24 必需真实门，不从头重做已实现模块。

**什么还没验证：** 目标工具链全量构建、完整宿主类型图、真实 DSH 安装／启动／浏览器认证、真实 SRV-01 登录／跨主体／伪造身份拒绝、真实 session 读取与 storage-domain/DB 强杀路径、ui-kit 五态截图，以及业务 owner 的运行任务收敛联验。

**新阻塞：** 回收者需要固定宿主和已授权测试环境；ui-kit rc.3 tarball／digest 缺失且视觉消费尚未实现。当前候选保持 PARTIAL，不解锁依赖它被 ACCEPTED 的下游。
