# peer 范围化 · 两内核安装验证（本机 2026-09-20，🧪 证据，不是用户 ACCEPTED）

**改动：** app-host rc.17 / usage rc.6 / core rc.18 的 `peerDependencies` 里 `@deepseek-ai/dsh-*` 从精确钉 `0.1.5-alpha.1` 改为 `>=0.1.5-alpha.1 <0.2.0`，`@deepseek-ai/cordis` 改 `>=4.0.2 <5`，`@deepseek-ai/schemastery` 改 `>=3.18.2 <4`；Vibe rc.14 精确 peer app-host rc.17；core `dependencies` 精确钉 usage rc.6 + app-host rc.17（自有链）。运行逻辑零改动。发布到本机私有 registry `http://127.0.0.1:4873`（`registry-dist-tags.txt`）。

**alpha1/（内核 `0.1.5-alpha.1`，`hanamesh-dsh-runtime/runtime`，`scripts/p1/fresh-home.sh` 隔离 `DSH_HOME`，随机回环端口）：** `dsh plugin --profile core add hanamesh-core@0.2.0-rc.18` exit 0（`plugin-add.log`）；`--dump-config` 恰三条 `hanamesh-core / hanamesh-usage / hanamesh-app-host`（`dump-config.yml`）；`boot.sh` 起宿主后 `/api/hanamesh/core/state` health `normal`，usage `satisfied 0.2.0-rc.6 serviceReady:true`，app-host `satisfied 0.1.0-rc.17`（`state.json`）；`/api/hanamesh/usage/health`、`/hanamesh/apps` 200（`routes.txt`）；`pnpm ls`：core → app-host rc.17 + usage rc.6 + zod 4.5.4 一份（`pnpm-ls.txt`）；`storages/` 三个 hanamesh 单元（`storages.txt`）；`boot.log` 无 error/duplicate/failed。

**rc2/（内核 `0.1.5-rc.2` = 用户官方 dsh-tauri 0.15.5 的运行时 `…/dependencies/dsh/node_modules/@deepseek-ai/dsh/lib/bin.js`，只读使用；`DSH_HOME=$(mktemp -d)`、`--port 34590`、`--no-open`）：** `version.txt` = `0.1.5-rc.2`；`--from-default-profile web --dump-config` 只写隔离目录；`plugin add hanamesh-core@0.2.0-rc.18` exit 0，三条 loader 条目，profile `dependencies` 只有 core、`bundles` 末尾 `hanamesh-core`；起宿主后 state/usage/apps 与 alpha.1 完全一致（`state.json`、`routes.txt`），首页含 `hanamesh-core/client.js` 与 `@hanamesh/dsh-app-host/client.js`；`storages/` 三个 hanamesh 单元写在隔离目录；SIGTERM 后 34590 释放。**`~/.dsh` 前后快照（mtime+size）零差异**（`dsh-home-diff.txt`）；3080 全程无监听（桌面壳自身日志最后一条为 16:27 JST「Harness stopped and port released for installer handoff」，早于本次），`~/.dsh/profiles/tauri` 未动。

**peer 提示的事实：** 两内核上 `dsh plugin add` 仍打印 pnpm `WARN Issues with peer dependencies found … missing peer @deepseek-ai/…@">=0.1.5-alpha.1 <0.2.0"`。这是已知 C20：DSH profile 是 `autoInstallPeers: false` 的 hoisted workspace，宿主自带的 `@deepseek-ai/*`/react 不在 profile manifest 里，pnpm 看不到它们，与 peer 写精确还是范围无关（rc.16 及之前的日志同样有）。范围化后消失的是「conflicting peer dependencies: zod」（rc.17 已对齐）；没有 `unmet peer` / `conflicting` 指向 HanaMesh 包。若要让这条 WARN 消失，需要 DSH 把宿主包写进 profile manifest 或开 `autoInstallPeers`，都不在插件侧。

**必须保留精确钉的例外：** 无。`@deepseek-ai/cordis` 在两内核上都是 `4.0.2`，`@deepseek-ai/schemastery` 都是 `3.18.2`，范围内实测只有这一个值；`>=4.0.2 <5` / `>=3.18.2 <4` 是按 law 写的「已实测起点 + 主版本上限」，不是对 4.x/3.x 其它版本的实测承诺。
